# 🚀 Исправление 400 ошибки при создании автокампании

## 📋 Проблема
При создании автокампании получаете ошибку 400: "each value in platforms must be one of the following values..."

## ✅ Что уже сделано
- ✅ Backend код обновлен (константы платформ в lowercase)
- ✅ Frontend код обновлен
- ✅ Проект успешно собран (`npm run build`)
- ✅ Валидация исправлена во всех DTO

## 🔧 Что нужно сделать
**Задеплоить обновленный backend на Vercel**

### Способ 1: Через Vercel CLI (быстрый)

```bash
# 1. Установить Vercel CLI (если не установлен)
npm i -g vercel

# 2. Перейти в папку backend
cd backend

# 3. Залогиниться
vercel login

# 4. Задеплоить
vercel --prod
```

**Важно:** При деплое выберите существующий проект (например, `influo-backend` или `influo-zxx9f2oxw-matve`).

### Способ 2: Через Git (автоматический)

Если у вас настроена интеграция Vercel с Git:

```bash
git add .
git commit -m "fix: update platform constants to lowercase"
git push
```

Vercel автоматически задеплоит изменения.

### Способ 3: Через Vercel Dashboard

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Найдите проект backend
3. Нажмите "Redeploy" на последнем деплое

## 🧪 Проверка после деплоя

После успешного деплоя попробуйте создать автокампанию через UI:

1. Откройте страницу "Автокампании"
2. Нажмите "Создать автокампанию"
3. Выберите "Мода и стиль" (или любую категорию)
4. Заполните форму
5. Нажмите "Создать"

**Ожидаемый результат:** Кампания создается успешно, ошибка 400 не появляется.

## 📊 Технические детали

### Изменения в backend:

```
backend/src/common/constants/
├── platforms.ts          ← Platform enum (lowercase values)
├── content-types.ts      ← ContentType enum
└── index.ts             ← Exports

backend/src/modules/*/dto/
├── create-auto-campaign.dto.ts      ← Uses Platform enum
├── create-influencer-card.dto.ts    ← Uses Platform enum
└── create-advertiser-card.dto.ts    ← Uses Platform enum
```

### До исправления:
```json
// Backend ожидал (старое)
{ "platforms": ["Instagram", "TikTok"] }
```

### После исправления:
```json
// Backend принимает (новое)
{ "platforms": ["instagram", "tiktok"] }
```

Frontend уже отправляет правильные lowercase значения, поэтому после деплоя backend все заработает!

## 🆘 Если не помогло

1. **Очистите кеш Vercel:**
   - Settings → Clear Cache
   - Redeploy

2. **Проверьте переменные окружения в Vercel:**
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `JWT_SECRET`

3. **Проверьте логи:**
   - Vercel Dashboard → Deployments → Function Logs

4. **Напишите мне, если проблема остается!**

---

**Примерное время деплоя:** 2-3 минуты

После деплоя создание автокампаний будет работать! 🎉
