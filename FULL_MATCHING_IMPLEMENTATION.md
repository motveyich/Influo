# ✅ Полная реализация автоматического подбора инфлюенсеров

## Что было сделано

Полностью переписана логика автоматического подбора инфлюенсеров на backend, восстановив функциональность из старой рабочей версии.

## Ключевые изменения

### 1. Вспомогательные функции (Helper Functions)

Добавлены функции для корректной работы с данными:

```typescript
// Нормализация строк для case-insensitive сравнения
private normalizeString(str: string): string

// Нормализация массивов
private normalizeArray(arr: string[]): string[]

// Проверка пересечения массивов (case-insensitive)
private arraysOverlap(arr1: string[], arr2: string[]): boolean

// Поиск цены для формата в pricing (case-insensitive)
private findPriceForFormat(pricing: Record<string, number>, format: string): number | null
```

### 2. Полная фильтрация инфлюенсеров

Реализовано **6 фильтров** вместо прежних 2:

#### ✅ Обязательные фильтры:

1. **Платформа** - карточка должна быть на нужной платформе
2. **Размер аудитории** - followers в диапазоне `audience_min` - `audience_max`
3. **Типы контента** - хотя бы один тип контента должен совпадать (case-insensitive)
4. **Бюджет** - цена формата должна быть в диапазоне `budget_min` - `budget_max`

#### ✅ Опциональные фильтры (если указано в кампании):

5. **Страны** - хотя бы одна страна из `topCountries` должна совпадать с `target_countries`
6. **Интересы аудитории** - хотя бы один интерес должен совпадать с `target_audience_interests`
7. **Категории продуктов** - категории кампании НЕ должны быть в `blacklistedProductCategories` инфлюенсера

### 3. Группировка по инфлюенсерам

Один инфлюенсер может иметь несколько карточек (разные платформы):
- Карточки группируются по `user_id`
- Для каждого инфлюенсера выбирается **лучшая комбинация** карточка + формат
- Отправляется только **один оффер** на инфлюенсера

### 4. Выбор формата и цены

Вместо усредненного бюджета:
- Для каждой карточки ищутся все подходящие форматы
- Форматы с ценами вне бюджета отфильтровываются
- Выбирается **самый дешевый** подходящий формат
- В оффер записывается **конкретная цена** из pricing карточки

### 5. Overbooking (перебронирование)

Применяется коэффициент **1.25** к целевому количеству:
- Если нужно 10 инфлюенсеров → отправляется 13 приглашений (10 × 1.25)
- Компенсирует возможные отказы
- Увеличивает шансы набрать нужное количество участников

### 6. Rate Limit (ограничение частоты)

Проверка перед отправкой каждого оффера:
- Между парой пользователей (advertiser ↔ influencer) может быть только **1 оффер в час**
- Проверяется наличие активных офферов (`pending`, `accepted`) за последний час
- При превышении лимита оффер пропускается

### 7. Улучшенное создание оффера

Оффер содержит точные данные:

```typescript
{
  // Основные поля
  influencer_id: string,
  advertiser_id: string,
  influencer_card_id: string,  // ID конкретной карточки
  auto_campaign_id: string,

  // Точная цена и формат
  amount: selectedPrice,  // Точная цена из pricing
  proposed_rate: selectedPrice,
  content_type: selectedFormat,  // Конкретный формат
  deliverables: [selectedFormat],

  // Метаданные
  details: {
    title, description,
    proposed_rate: selectedPrice,
    currency: 'RUB',
    deliverables: [selectedFormat],
    platform: cardPlatform,
    timeline: { start_date, end_date }
  },

  metadata: {
    isAutoCampaign: true,
    autoCampaignId,
    sourceType: 'auto_campaign',
    selectedFormat,
    calculatedPrice: selectedPrice,
    pricePerFollower,
    enableChat
  }
}
```

### 8. Детальное логирование

Логируется каждый этап процесса:

```
========================================
🚀 Starting auto-offer distribution for campaign {id}
📊 Campaign criteria:
   - Platforms: [instagram]
   - Audience: 1000-10000
   - Budget: 100-500 RUB
   - Target count: 10
   - Content types: [Post, Story]
   - Target countries: (not set)
   - Target interests: (not set)
   - Product categories: (not set)

📋 Querying database for active cards...
   - Platforms: [instagram]
   - is_active = true
   - is_deleted = false

✅ Found 50 active influencer cards
✓ Grouped into 45 unique influencers

📊 Filtering results:
   Total cards: 50
   Filtered by audience: 15
   Filtered by content types: 10
   Filtered by budget: 8
   ✅ Final matches: 17 influencers

📈 Overbooking calculation:
   Target: 10 influencers
   With overbooking (+25%): 13
   Available: 17
   Will invite: 13

📤 Sending offers...
   ✅ Sent offer #1 to influencer xxx (Post @ 300 RUB)
   ✅ Sent offer #2 to influencer yyy (Story @ 150 RUB)
   ⏱️ Rate limit: skipping influencer zzz
   ...

📊 Final results:
   ✅ Sent: 12
   ⏱️ Skipped (rate limit): 1
   ❌ Failed: 0

✅ Campaign counters updated: 12 offers sent
========================================
```

## Алгоритм подбора (пошагово)

```
1. Загрузить активные карточки с фильтром по платформам
   ↓
2. Сгруппировать карточки по user_id
   ↓
3. Для каждого инфлюенсера:
   a. Перебрать все его карточки
   b. Для каждой карточки:
      - Проверить followers в диапазоне ✓
      - Найти совпадающие типы контента ✓
      - Для каждого типа проверить цену в бюджете ✓
      - Проверить страны (если указаны) ✓
      - Проверить интересы (если указаны) ✓
      - Проверить blacklist категорий (если указаны) ✓
      - Выбрать самый дешевый формат ✓
   c. Сохранить лучшую карточку для инфлюенсера
   ↓
4. Отсортировать по price per follower (лучшая цена первой)
   ↓
5. Применить overbooking: взять target × 1.25 инфлюенсеров
   ↓
6. Для каждого инфлюенсера:
   a. Проверить rate limit (1 час между офферами)
   b. Создать оффер с конкретными параметрами
   c. Обновить счетчик sent_offers_count
```

## Структура данных

### Карточка инфлюенсера в БД:

```typescript
{
  id: string,
  user_id: string,
  platform: 'instagram' | 'youtube' | 'tiktok',
  is_active: boolean,
  is_deleted: boolean,

  reach: {
    followers: number,
    engagementRate: number,
    averageViews: number
  },

  service_details: {
    contentTypes: ['Post', 'Story', 'Reel'],
    pricing: {
      'Post': 1000,
      'Story': 500,
      'Reel': 1500
    },
    blacklistedProductCategories: ['Alcohol', 'Gambling']
  },

  audience_demographics: {
    topCountries: ['Russia', 'USA'] | { 'Russia': 80, 'USA': 20 },
    interests: ['Fashion', 'Travel'] | { 'Fashion': 60, 'Travel': 40 }
  }
}
```

### Кампания:

```typescript
{
  platforms: ['instagram'],
  content_types: ['Post', 'Story'],
  audience_min: 1000,
  audience_max: 10000,
  budget_min: 100,
  budget_max: 500,
  target_influencers_count: 10,

  // Опциональные фильтры
  target_countries: ['Russia', 'USA'],
  target_audience_interests: ['Fashion', 'Technology'],
  product_categories: ['Electronics', 'Clothing']
}
```

## Ключевые особенности

### ✅ Case-insensitive сравнение
Все строковые поля (платформы, типы контента, страны, интересы) сравниваются без учета регистра.

### ✅ Обработка разных форматов данных
- `topCountries`: массив строк или объект `{country: percentage}`
- `interests`: массив строк или объект `{interest: percentage}`

### ✅ Один оффер на инфлюенсера
Даже если у инфлюенсера 5 карточек на разных платформах - отправляется только 1 оффер с лучшими условиями.

### ✅ Точные цены
Вместо усредненного бюджета используются реальные цены из `pricing` карточки.

### ✅ Rate limit
Защита от спама - между парой пользователей максимум 1 оффер в час.

### ✅ Детальная статистика
Каждый этап фильтрации логируется для отладки.

## Что изменилось

### До:
- Фильтрация только по платформе и аудитории
- Усредненный бюджет
- Нет группировки по инфлюенсерам
- Нет rate limit
- Нет overbooking
- Минимальное логирование

### После:
- 6-7 фильтров (платформа, аудитория, контент, бюджет, страны, интересы, категории)
- Точные цены из pricing карточек
- Группировка и выбор лучшей карточки
- Rate limit 1 час
- Overbooking 25%
- Подробное логирование каждого шага

## Тестирование

### Проверка работы через логи:

1. Создайте автокампанию через UI
2. Откройте логи backend (Vercel Logs)
3. Найдите сообщение `🚀 Starting auto-offer distribution`
4. Проверьте:
   - Критерии кампании
   - Количество найденных карточек
   - Результаты фильтрации
   - Количество отправленных офферов

### Проверка через БД:

```sql
-- Офферы из кампании
SELECT * FROM offers
WHERE auto_campaign_id = 'campaign_id'
ORDER BY created_at DESC;

-- Проверка полей
SELECT
  influencer_id,
  content_type,
  amount,
  metadata->>'selectedFormat',
  metadata->>'calculatedPrice'
FROM offers
WHERE auto_campaign_id = 'campaign_id';

-- Счетчики кампании
SELECT
  title,
  sent_offers_count,
  accepted_offers_count
FROM auto_campaigns
WHERE id = 'campaign_id';
```

## Деплой

Оба проекта собраны успешно:

```bash
# Backend
cd backend
vercel --prod

# Frontend (если были изменения)
npm run build
vercel --prod
```

## Статус

✅ Вспомогательные функции добавлены
✅ Полная фильтрация реализована (6-7 фильтров)
✅ Группировка по инфлюенсерам добавлена
✅ Rate limit реализован
✅ Overbooking добавлен
✅ Улучшено создание офферов
✅ Детальное логирование
✅ Backend собран
✅ Frontend собран
⏳ Требуется деплой на production

После деплоя система будет автоматически подбирать инфлюенсеров по всем критериям кампании!
